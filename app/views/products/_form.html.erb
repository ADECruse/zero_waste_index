<%= form_with(model: product, local: true) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :item %>
    <%= form.text_field :item, id: :product_item, placeholder: "Name..", class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :plastic_free %>
    <%= form.text_field :plastic_free, id: :product_plastic_free, placeholder: "True or False", class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :description %>
    <%= form.text_field :description, id: :product_description, placeholder: "Write a short description here...", class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :packaging %>
    <%= form.text_field :packaging, id: :product_packaging, placeholder: "Name..", class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :rating %>
    <%= form.number_field :rating, id: :product_rating, in: 1..5, step: 1, class: "form-control" %>
  </div>

  <!-- <div class="form-group">
    <%= form.label :rating %>
    <%= form.select(:rating, '<option>1</option>
      <option>2</option>
      <option>3</option>
      <option>4</option>
      <option>5</option>', id: :product_rating, class: "form-control" )  %>
  </div> -->

  <div class="form-group">
    <%= form.label :picture %>
    <%= form.text_field :picture, id: :product_picture, placeholder: "apple.jpg", class: "form-control" %>
  </div>

  <div class="form-check">
    <%= form.collection_check_boxes :location_ids, Location.all, :id, :store do |m| %>
    <%= m.check_box %> <%= m.label class: "form-check-label"%>
    <% end %>
  </div>
  <!-- field where you select which location a product belongs to -->

  <div class="actions text-center">
    <%= form.submit class: "btn btn-light"%>
  </div>
<% end %>
